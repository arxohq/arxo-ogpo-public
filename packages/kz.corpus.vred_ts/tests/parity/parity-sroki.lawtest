language "law.core" version "0.1";
package kz.corpus.vred_ts version "0.1.0";
namespace "urn:kz:corpus:clir:vred-ts";

// GENERATED by compare.py --emit from tools/catala/cases.json — do not edit by hand.
// Expectations are the cases.json reference set (per the Rules text and the
// 2026 calendar), shared with the Catala model tools/catala/vred_ts.catala_en.

context profile KZ_VRED_TS_DEADLINES {
    deadline_policy {
            start_count next_day;
            include_end true;
            roll next_working_day;
        }
}

// S01 — cl. 3 para. 3: Tue 1 September: Wed 2 (1), Thu 3 (2), Fri 4 (3).
test "urn:query:vred-parity:S01" {
    given {
        context extends KZ_VRED_TS_DEADLINES {
            decision_time @2026-09-01T12:00:00+05:00;
            knowledge_time @2026-09-01T12:00:00+05:00;
            legal_time @2026-09-01;
            timezone "Asia/Qyzylorda";
            calendar "urn:kz:corpus:clir:official-calendar#snapshot-2026";
        }
        assert poterpevshiy_po_raschetu(entity_ref("urn:parity:vred:raschet-1"), entity_ref("urn:parity:vred:poterpevshiy-1")) { id "S01-a0"; origin case_input; }
        assert otchet_poluchen_poterpevshim(entity_ref("urn:parity:vred:raschet-1"), @2026-09-01) { id "S01-a1"; origin case_input; }
    }
    evaluate collect day: Date where predel_otmetki_poterpevshego(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(1);
    evaluate truth(predel_otmetki_poterpevshego(entity_ref("urn:parity:vred:raschet-1"), @2026-09-04));
    expect truth_status == TRUE_ONLY;
    evaluate collect day: Date where predel_otveta_strakhovshchika(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(0);
    evaluate collect day: Date where predel_predostavleniya_otcheta(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(0);
    evaluate truth(otchet_ne_predostavlen_v_srok(entity_ref("urn:parity:vred:raschet-1")));
    expect truth_status == NEITHER;
    evaluate truth(pravo_na_vyplatu_po_punktu_3_1_stati_22_zakona(entity_ref("urn:parity:vred:poterpevshiy-1"), entity_ref("urn:parity:vred:raschet-1")));
    expect truth_status == NEITHER;
}

// S02 — cl. 3 para. 3: Fri 6 March: Sat 7, Sun 8 (holiday), Mon 9 (shifted day off) do not count; Tue 10 (1), Wed 11 (2), Thu 12 (3).
test "urn:query:vred-parity:S02" {
    given {
        context extends KZ_VRED_TS_DEADLINES {
            decision_time @2026-09-01T12:00:00+05:00;
            knowledge_time @2026-09-01T12:00:00+05:00;
            legal_time @2026-09-01;
            timezone "Asia/Qyzylorda";
            calendar "urn:kz:corpus:clir:official-calendar#snapshot-2026";
        }
        assert poterpevshiy_po_raschetu(entity_ref("urn:parity:vred:raschet-1"), entity_ref("urn:parity:vred:poterpevshiy-1")) { id "S02-a0"; origin case_input; }
        assert otchet_poluchen_poterpevshim(entity_ref("urn:parity:vred:raschet-1"), @2026-03-06) { id "S02-a1"; origin case_input; }
    }
    evaluate collect day: Date where predel_otmetki_poterpevshego(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(1);
    evaluate truth(predel_otmetki_poterpevshego(entity_ref("urn:parity:vred:raschet-1"), @2026-03-12));
    expect truth_status == TRUE_ONLY;
    evaluate collect day: Date where predel_otveta_strakhovshchika(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(0);
    evaluate collect day: Date where predel_predostavleniya_otcheta(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(0);
    evaluate truth(otchet_ne_predostavlen_v_srok(entity_ref("urn:parity:vred:raschet-1")));
    expect truth_status == NEITHER;
    evaluate truth(pravo_na_vyplatu_po_punktu_3_1_stati_22_zakona(entity_ref("urn:parity:vred:poterpevshiy-1"), entity_ref("urn:parity:vred:raschet-1")));
    expect truth_status == NEITHER;
}

// S03 — cl. 3 para. 6: Thu 19 March: Fri 20 (1); 21-25 March are non-working (Nauryz and shifts); Thu 26 (2), Fri 27 (3); weekend; Mon 30 (4), Tue 31 (5).
test "urn:query:vred-parity:S03" {
    given {
        context extends KZ_VRED_TS_DEADLINES {
            decision_time @2026-09-01T12:00:00+05:00;
            knowledge_time @2026-09-01T12:00:00+05:00;
            legal_time @2026-09-01;
            timezone "Asia/Qyzylorda";
            calendar "urn:kz:corpus:clir:official-calendar#snapshot-2026";
        }
        assert poterpevshiy_po_raschetu(entity_ref("urn:parity:vred:raschet-1"), entity_ref("urn:parity:vred:poterpevshiy-1")) { id "S03-a0"; origin case_input; }
        assert osmotr_osushchestvlen_strakhovshchikom(entity_ref("urn:parity:vred:raschet-1"), @2026-03-19) { id "S03-a1"; origin case_input; }
    }
    evaluate collect day: Date where predel_otmetki_poterpevshego(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(0);
    evaluate collect day: Date where predel_otveta_strakhovshchika(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(0);
    evaluate collect day: Date where predel_predostavleniya_otcheta(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(1);
    evaluate truth(predel_predostavleniya_otcheta(entity_ref("urn:parity:vred:raschet-1"), @2026-03-31));
    expect truth_status == TRUE_ONLY;
    evaluate truth(otchet_ne_predostavlen_v_srok(entity_ref("urn:parity:vred:raschet-1")));
    expect truth_status == NEITHER;
    evaluate truth(pravo_na_vyplatu_po_punktu_3_1_stati_22_zakona(entity_ref("urn:parity:vred:poterpevshiy-1"), entity_ref("urn:parity:vred:raschet-1")));
    expect truth_status == NEITHER;
}

// S04 — cl. 3 para. 4: Wed 15 April: Thu 16 (1), Fri 17 (2), Mon 20 (3).
test "urn:query:vred-parity:S04" {
    given {
        context extends KZ_VRED_TS_DEADLINES {
            decision_time @2026-09-01T12:00:00+05:00;
            knowledge_time @2026-09-01T12:00:00+05:00;
            legal_time @2026-09-01;
            timezone "Asia/Qyzylorda";
            calendar "urn:kz:corpus:clir:official-calendar#snapshot-2026";
        }
        assert poterpevshiy_po_raschetu(entity_ref("urn:parity:vred:raschet-1"), entity_ref("urn:parity:vred:poterpevshiy-1")) { id "S04-a0"; origin case_input; }
        assert otmetka_nesoglasiya_poluchena_strakhovshchikom(entity_ref("urn:parity:vred:raschet-1"), @2026-04-15) { id "S04-a1"; origin case_input; }
    }
    evaluate collect day: Date where predel_otmetki_poterpevshego(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(0);
    evaluate collect day: Date where predel_otveta_strakhovshchika(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(1);
    evaluate truth(predel_otveta_strakhovshchika(entity_ref("urn:parity:vred:raschet-1"), @2026-04-20));
    expect truth_status == TRUE_ONLY;
    evaluate collect day: Date where predel_predostavleniya_otcheta(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(0);
    evaluate truth(otchet_ne_predostavlen_v_srok(entity_ref("urn:parity:vred:raschet-1")));
    expect truth_status == NEITHER;
    evaluate truth(pravo_na_vyplatu_po_punktu_3_1_stati_22_zakona(entity_ref("urn:parity:vred:poterpevshiy-1"), entity_ref("urn:parity:vred:raschet-1")));
    expect truth_status == NEITHER;
}

// S05 — cl. 3 para. 6: Report deadline — Tue 8 September (2, 3, 4, 7, 8); the report was received exactly on the last day — on time. Mark deadline from 8 September: 9 (1), 10 (2), 11 (3).
test "urn:query:vred-parity:S05" {
    given {
        context extends KZ_VRED_TS_DEADLINES {
            decision_time @2026-09-01T12:00:00+05:00;
            knowledge_time @2026-09-01T12:00:00+05:00;
            legal_time @2026-09-01;
            timezone "Asia/Qyzylorda";
            calendar "urn:kz:corpus:clir:official-calendar#snapshot-2026";
        }
        assert poterpevshiy_po_raschetu(entity_ref("urn:parity:vred:raschet-1"), entity_ref("urn:parity:vred:poterpevshiy-1")) { id "S05-a0"; origin case_input; }
        assert otchet_poluchen_poterpevshim(entity_ref("urn:parity:vred:raschet-1"), @2026-09-08) { id "S05-a1"; origin case_input; }
        assert osmotr_osushchestvlen_strakhovshchikom(entity_ref("urn:parity:vred:raschet-1"), @2026-09-01) { id "S05-a2"; origin case_input; }
    }
    evaluate collect day: Date where predel_otmetki_poterpevshego(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(1);
    evaluate truth(predel_otmetki_poterpevshego(entity_ref("urn:parity:vred:raschet-1"), @2026-09-11));
    expect truth_status == TRUE_ONLY;
    evaluate collect day: Date where predel_otveta_strakhovshchika(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(0);
    evaluate collect day: Date where predel_predostavleniya_otcheta(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(1);
    evaluate truth(predel_predostavleniya_otcheta(entity_ref("urn:parity:vred:raschet-1"), @2026-09-08));
    expect truth_status == TRUE_ONLY;
    evaluate truth(otchet_ne_predostavlen_v_srok(entity_ref("urn:parity:vred:raschet-1")));
    expect truth_status == NEITHER;
    evaluate truth(pravo_na_vyplatu_po_punktu_3_1_stati_22_zakona(entity_ref("urn:parity:vred:poterpevshiy-1"), entity_ref("urn:parity:vred:raschet-1")));
    expect truth_status == NEITHER;
}

// S06 — cl. 3 para. 6: Received on 9 September — after the 8 September deadline: late, giving the right to payment under cl. 3-1 art. 22 of the Law. Mark deadline from Wed 9: Thu 10 (1), Fri 11 (2), Mon 14 (3).
test "urn:query:vred-parity:S06" {
    given {
        context extends KZ_VRED_TS_DEADLINES {
            decision_time @2026-09-01T12:00:00+05:00;
            knowledge_time @2026-09-01T12:00:00+05:00;
            legal_time @2026-09-01;
            timezone "Asia/Qyzylorda";
            calendar "urn:kz:corpus:clir:official-calendar#snapshot-2026";
        }
        assert poterpevshiy_po_raschetu(entity_ref("urn:parity:vred:raschet-1"), entity_ref("urn:parity:vred:poterpevshiy-1")) { id "S06-a0"; origin case_input; }
        assert otchet_poluchen_poterpevshim(entity_ref("urn:parity:vred:raschet-1"), @2026-09-09) { id "S06-a1"; origin case_input; }
        assert osmotr_osushchestvlen_strakhovshchikom(entity_ref("urn:parity:vred:raschet-1"), @2026-09-01) { id "S06-a2"; origin case_input; }
    }
    evaluate collect day: Date where predel_otmetki_poterpevshego(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(1);
    evaluate truth(predel_otmetki_poterpevshego(entity_ref("urn:parity:vred:raschet-1"), @2026-09-14));
    expect truth_status == TRUE_ONLY;
    evaluate collect day: Date where predel_otveta_strakhovshchika(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(0);
    evaluate collect day: Date where predel_predostavleniya_otcheta(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(1);
    evaluate truth(predel_predostavleniya_otcheta(entity_ref("urn:parity:vred:raschet-1"), @2026-09-08));
    expect truth_status == TRUE_ONLY;
    evaluate truth(otchet_ne_predostavlen_v_srok(entity_ref("urn:parity:vred:raschet-1")));
    expect truth_status == TRUE_ONLY;
    evaluate truth(pravo_na_vyplatu_po_punktu_3_1_stati_22_zakona(entity_ref("urn:parity:vred:poterpevshiy-1"), entity_ref("urn:parity:vred:raschet-1")));
    expect truth_status == TRUE_ONLY;
}

// S07 — cl. 3 para. 6: Not provided, but the review day is the last day of the deadline — the deadline has not yet been missed.
test "urn:query:vred-parity:S07" {
    given {
        context extends KZ_VRED_TS_DEADLINES {
            decision_time @2026-09-01T12:00:00+05:00;
            knowledge_time @2026-09-01T12:00:00+05:00;
            legal_time @2026-09-01;
            timezone "Asia/Qyzylorda";
            calendar "urn:kz:corpus:clir:official-calendar#snapshot-2026";
        }
        assert poterpevshiy_po_raschetu(entity_ref("urn:parity:vred:raschet-1"), entity_ref("urn:parity:vred:poterpevshiy-1")) { id "S07-a0"; origin case_input; }
        assert osmotr_osushchestvlen_strakhovshchikom(entity_ref("urn:parity:vred:raschet-1"), @2026-09-01) { id "S07-a1"; origin case_input; }
        assert not otchet_predostavlen_poterpevshemu(entity_ref("urn:parity:vred:raschet-1")) { id "S07-a2"; origin case_input; }
        assert den_rassmotreniya_dela(entity_ref("urn:parity:vred:raschet-1"), @2026-09-08) { id "S07-a3"; origin case_input; }
    }
    evaluate collect day: Date where predel_otmetki_poterpevshego(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(0);
    evaluate collect day: Date where predel_otveta_strakhovshchika(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(0);
    evaluate collect day: Date where predel_predostavleniya_otcheta(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(1);
    evaluate truth(predel_predostavleniya_otcheta(entity_ref("urn:parity:vred:raschet-1"), @2026-09-08));
    expect truth_status == TRUE_ONLY;
    evaluate truth(otchet_ne_predostavlen_v_srok(entity_ref("urn:parity:vred:raschet-1")));
    expect truth_status == NEITHER;
    evaluate truth(pravo_na_vyplatu_po_punktu_3_1_stati_22_zakona(entity_ref("urn:parity:vred:poterpevshiy-1"), entity_ref("urn:parity:vred:raschet-1")));
    expect truth_status == NEITHER;
}

// S08 — cl. 3 para. 6: The review day is after the deadline — not provided on time.
test "urn:query:vred-parity:S08" {
    given {
        context extends KZ_VRED_TS_DEADLINES {
            decision_time @2026-09-01T12:00:00+05:00;
            knowledge_time @2026-09-01T12:00:00+05:00;
            legal_time @2026-09-01;
            timezone "Asia/Qyzylorda";
            calendar "urn:kz:corpus:clir:official-calendar#snapshot-2026";
        }
        assert poterpevshiy_po_raschetu(entity_ref("urn:parity:vred:raschet-1"), entity_ref("urn:parity:vred:poterpevshiy-1")) { id "S08-a0"; origin case_input; }
        assert osmotr_osushchestvlen_strakhovshchikom(entity_ref("urn:parity:vred:raschet-1"), @2026-09-01) { id "S08-a1"; origin case_input; }
        assert not otchet_predostavlen_poterpevshemu(entity_ref("urn:parity:vred:raschet-1")) { id "S08-a2"; origin case_input; }
        assert den_rassmotreniya_dela(entity_ref("urn:parity:vred:raschet-1"), @2026-09-09) { id "S08-a3"; origin case_input; }
    }
    evaluate collect day: Date where predel_otmetki_poterpevshego(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(0);
    evaluate collect day: Date where predel_otveta_strakhovshchika(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(0);
    evaluate collect day: Date where predel_predostavleniya_otcheta(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(1);
    evaluate truth(predel_predostavleniya_otcheta(entity_ref("urn:parity:vred:raschet-1"), @2026-09-08));
    expect truth_status == TRUE_ONLY;
    evaluate truth(otchet_ne_predostavlen_v_srok(entity_ref("urn:parity:vred:raschet-1")));
    expect truth_status == TRUE_ONLY;
    evaluate truth(pravo_na_vyplatu_po_punktu_3_1_stati_22_zakona(entity_ref("urn:parity:vred:poterpevshiy-1"), entity_ref("urn:parity:vred:raschet-1")));
    expect truth_status == TRUE_ONLY;
}

// S09 — cl. 3-1: Simplified processing: none of the clause 3 deadlines apply (reading — all three deadlines; report question Q1), nor do the consequences.
test "urn:query:vred-parity:S09" {
    given {
        context extends KZ_VRED_TS_DEADLINES {
            decision_time @2026-09-01T12:00:00+05:00;
            knowledge_time @2026-09-01T12:00:00+05:00;
            legal_time @2026-09-01;
            timezone "Asia/Qyzylorda";
            calendar "urn:kz:corpus:clir:official-calendar#snapshot-2026";
        }
        assert poterpevshiy_po_raschetu(entity_ref("urn:parity:vred:raschet-1"), entity_ref("urn:parity:vred:poterpevshiy-1")) { id "S09-a0"; origin case_input; }
        assert uproshchennoe_oformlenie_proisshestviya(entity_ref("urn:parity:vred:raschet-1")) { id "S09-a1"; origin case_input; }
        assert otchet_poluchen_poterpevshim(entity_ref("urn:parity:vred:raschet-1"), @2026-09-09) { id "S09-a2"; origin case_input; }
        assert osmotr_osushchestvlen_strakhovshchikom(entity_ref("urn:parity:vred:raschet-1"), @2026-09-01) { id "S09-a3"; origin case_input; }
    }
    evaluate collect day: Date where predel_otmetki_poterpevshego(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(0);
    evaluate collect day: Date where predel_otveta_strakhovshchika(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(0);
    evaluate collect day: Date where predel_predostavleniya_otcheta(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(0);
    evaluate truth(otchet_ne_predostavlen_v_srok(entity_ref("urn:parity:vred:raschet-1")));
    expect truth_status == NEITHER;
    evaluate truth(pravo_na_vyplatu_po_punktu_3_1_stati_22_zakona(entity_ref("urn:parity:vred:poterpevshiy-1"), entity_ref("urn:parity:vred:raschet-1")));
    expect truth_status == NEITHER;
}

// S10 — cl. 3; §85: Wed 30 December: Thu 31 (1), then 2027 falls outside the snapshot's coverage — the deadline cannot be computed.
test "urn:query:vred-parity:S10" {
    given {
        context extends KZ_VRED_TS_DEADLINES {
            decision_time @2026-09-01T12:00:00+05:00;
            knowledge_time @2026-09-01T12:00:00+05:00;
            legal_time @2026-09-01;
            timezone "Asia/Qyzylorda";
            calendar "urn:kz:corpus:clir:official-calendar#snapshot-2026";
        }
        assert poterpevshiy_po_raschetu(entity_ref("urn:parity:vred:raschet-1"), entity_ref("urn:parity:vred:poterpevshiy-1")) { id "S10-a0"; origin case_input; }
        assert otchet_poluchen_poterpevshim(entity_ref("urn:parity:vred:raschet-1"), @2026-12-30) { id "S10-a1"; origin case_input; }
    }
    evaluate collect day: Date where predel_otmetki_poterpevshego(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(0);
    evaluate collect day: Date where predel_otveta_strakhovshchika(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(0);
    evaluate collect day: Date where predel_predostavleniya_otcheta(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(0);
    evaluate truth(otchet_ne_predostavlen_v_srok(entity_ref("urn:parity:vred:raschet-1")));
    expect truth_status == NEITHER;
    evaluate truth(pravo_na_vyplatu_po_punktu_3_1_stati_22_zakona(entity_ref("urn:parity:vred:poterpevshiy-1"), entity_ref("urn:parity:vred:raschet-1")));
    expect truth_status == NEITHER;
}

// S11 — cl. 3 para. 6: Thu 24 December: Fri 25 (1), Mon 28 (2), Tue 29 (3), Wed 30 (4), Thu 31 (5) — the last day of coverage.
test "urn:query:vred-parity:S11" {
    given {
        context extends KZ_VRED_TS_DEADLINES {
            decision_time @2026-09-01T12:00:00+05:00;
            knowledge_time @2026-09-01T12:00:00+05:00;
            legal_time @2026-09-01;
            timezone "Asia/Qyzylorda";
            calendar "urn:kz:corpus:clir:official-calendar#snapshot-2026";
        }
        assert poterpevshiy_po_raschetu(entity_ref("urn:parity:vred:raschet-1"), entity_ref("urn:parity:vred:poterpevshiy-1")) { id "S11-a0"; origin case_input; }
        assert osmotr_osushchestvlen_strakhovshchikom(entity_ref("urn:parity:vred:raschet-1"), @2026-12-24) { id "S11-a1"; origin case_input; }
    }
    evaluate collect day: Date where predel_otmetki_poterpevshego(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(0);
    evaluate collect day: Date where predel_otveta_strakhovshchika(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(0);
    evaluate collect day: Date where predel_predostavleniya_otcheta(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(1);
    evaluate truth(predel_predostavleniya_otcheta(entity_ref("urn:parity:vred:raschet-1"), @2026-12-31));
    expect truth_status == TRUE_ONLY;
    evaluate truth(otchet_ne_predostavlen_v_srok(entity_ref("urn:parity:vred:raschet-1")));
    expect truth_status == NEITHER;
    evaluate truth(pravo_na_vyplatu_po_punktu_3_1_stati_22_zakona(entity_ref("urn:parity:vred:poterpevshiy-1"), entity_ref("urn:parity:vred:raschet-1")));
    expect truth_status == NEITHER;
}

// S12 — cl. 3 para. 6; §85: Fri 25 December: 28 (1), 29 (2), 30 (3), 31 (4), the fifth falls in 2027, outside coverage.
test "urn:query:vred-parity:S12" {
    given {
        context extends KZ_VRED_TS_DEADLINES {
            decision_time @2026-09-01T12:00:00+05:00;
            knowledge_time @2026-09-01T12:00:00+05:00;
            legal_time @2026-09-01;
            timezone "Asia/Qyzylorda";
            calendar "urn:kz:corpus:clir:official-calendar#snapshot-2026";
        }
        assert poterpevshiy_po_raschetu(entity_ref("urn:parity:vred:raschet-1"), entity_ref("urn:parity:vred:poterpevshiy-1")) { id "S12-a0"; origin case_input; }
        assert osmotr_osushchestvlen_strakhovshchikom(entity_ref("urn:parity:vred:raschet-1"), @2026-12-25) { id "S12-a1"; origin case_input; }
    }
    evaluate collect day: Date where predel_otmetki_poterpevshego(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(0);
    evaluate collect day: Date where predel_otveta_strakhovshchika(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(0);
    evaluate collect day: Date where predel_predostavleniya_otcheta(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(0);
    evaluate truth(otchet_ne_predostavlen_v_srok(entity_ref("urn:parity:vred:raschet-1")));
    expect truth_status == NEITHER;
    evaluate truth(pravo_na_vyplatu_po_punktu_3_1_stati_22_zakona(entity_ref("urn:parity:vred:poterpevshiy-1"), entity_ref("urn:parity:vred:raschet-1")));
    expect truth_status == NEITHER;
}

// S13 — cl. 3 para. 3: Wed 6 May: Thu 7 (holiday), Fri 8 (1), Sat 9 (holiday), Sun 10, Mon 11 (shifted day off), Tue 12 (2), Wed 13 (3).
test "urn:query:vred-parity:S13" {
    given {
        context extends KZ_VRED_TS_DEADLINES {
            decision_time @2026-09-01T12:00:00+05:00;
            knowledge_time @2026-09-01T12:00:00+05:00;
            legal_time @2026-09-01;
            timezone "Asia/Qyzylorda";
            calendar "urn:kz:corpus:clir:official-calendar#snapshot-2026";
        }
        assert poterpevshiy_po_raschetu(entity_ref("urn:parity:vred:raschet-1"), entity_ref("urn:parity:vred:poterpevshiy-1")) { id "S13-a0"; origin case_input; }
        assert otchet_poluchen_poterpevshim(entity_ref("urn:parity:vred:raschet-1"), @2026-05-06) { id "S13-a1"; origin case_input; }
    }
    evaluate collect day: Date where predel_otmetki_poterpevshego(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(1);
    evaluate truth(predel_otmetki_poterpevshego(entity_ref("urn:parity:vred:raschet-1"), @2026-05-13));
    expect truth_status == TRUE_ONLY;
    evaluate collect day: Date where predel_otveta_strakhovshchika(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(0);
    evaluate collect day: Date where predel_predostavleniya_otcheta(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(0);
    evaluate truth(otchet_ne_predostavlen_v_srok(entity_ref("urn:parity:vred:raschet-1")));
    expect truth_status == NEITHER;
    evaluate truth(pravo_na_vyplatu_po_punktu_3_1_stati_22_zakona(entity_ref("urn:parity:vred:poterpevshiy-1"), entity_ref("urn:parity:vred:raschet-1")));
    expect truth_status == NEITHER;
}

// S14 — cl. 3 para. 6: Inspection on the 1 January holiday: 2 (holiday), 3-4 weekend; Mon 5 (1), 6 (2), 7 (3), 8 (4), Fri 9 (5).
test "urn:query:vred-parity:S14" {
    given {
        context extends KZ_VRED_TS_DEADLINES {
            decision_time @2026-09-01T12:00:00+05:00;
            knowledge_time @2026-09-01T12:00:00+05:00;
            legal_time @2026-09-01;
            timezone "Asia/Qyzylorda";
            calendar "urn:kz:corpus:clir:official-calendar#snapshot-2026";
        }
        assert poterpevshiy_po_raschetu(entity_ref("urn:parity:vred:raschet-1"), entity_ref("urn:parity:vred:poterpevshiy-1")) { id "S14-a0"; origin case_input; }
        assert osmotr_osushchestvlen_strakhovshchikom(entity_ref("urn:parity:vred:raschet-1"), @2026-01-01) { id "S14-a1"; origin case_input; }
    }
    evaluate collect day: Date where predel_otmetki_poterpevshego(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(0);
    evaluate collect day: Date where predel_otveta_strakhovshchika(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(0);
    evaluate collect day: Date where predel_predostavleniya_otcheta(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(1);
    evaluate truth(predel_predostavleniya_otcheta(entity_ref("urn:parity:vred:raschet-1"), @2026-01-09));
    expect truth_status == TRUE_ONLY;
    evaluate truth(otchet_ne_predostavlen_v_srok(entity_ref("urn:parity:vred:raschet-1")));
    expect truth_status == NEITHER;
    evaluate truth(pravo_na_vyplatu_po_punktu_3_1_stati_22_zakona(entity_ref("urn:parity:vred:poterpevshiy-1"), entity_ref("urn:parity:vred:raschet-1")));
    expect truth_status == NEITHER;
}

// S15 — cl. 3 para. 4: Fri 23 October: Sat 24, Sun 25 (holiday), Mon 26 (shifted day off); Tue 27 (1), Wed 28 (2), Thu 29 (3).
test "urn:query:vred-parity:S15" {
    given {
        context extends KZ_VRED_TS_DEADLINES {
            decision_time @2026-09-01T12:00:00+05:00;
            knowledge_time @2026-09-01T12:00:00+05:00;
            legal_time @2026-09-01;
            timezone "Asia/Qyzylorda";
            calendar "urn:kz:corpus:clir:official-calendar#snapshot-2026";
        }
        assert poterpevshiy_po_raschetu(entity_ref("urn:parity:vred:raschet-1"), entity_ref("urn:parity:vred:poterpevshiy-1")) { id "S15-a0"; origin case_input; }
        assert otmetka_nesoglasiya_poluchena_strakhovshchikom(entity_ref("urn:parity:vred:raschet-1"), @2026-10-23) { id "S15-a1"; origin case_input; }
    }
    evaluate collect day: Date where predel_otmetki_poterpevshego(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(0);
    evaluate collect day: Date where predel_otveta_strakhovshchika(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(1);
    evaluate truth(predel_otveta_strakhovshchika(entity_ref("urn:parity:vred:raschet-1"), @2026-10-29));
    expect truth_status == TRUE_ONLY;
    evaluate collect day: Date where predel_predostavleniya_otcheta(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(0);
    evaluate truth(otchet_ne_predostavlen_v_srok(entity_ref("urn:parity:vred:raschet-1")));
    expect truth_status == NEITHER;
    evaluate truth(pravo_na_vyplatu_po_punktu_3_1_stati_22_zakona(entity_ref("urn:parity:vred:poterpevshiy-1"), entity_ref("urn:parity:vred:raschet-1")));
    expect truth_status == NEITHER;
}

// S16 — cl. 3; §85: The event date is before the snapshot's coverage begins — the deadline cannot be computed. The date was shifted from 31.12.2025 to 31.12.2024 (12.09.2026): the official §85 snapshot was extended to [2025-01-01, 2027-01-01), and the earlier date fell WITHIN coverage — the boundary remains but now falls earlier. The case's meaning (an event before coverage begins) is preserved verbatim.
test "urn:query:vred-parity:S16" {
    given {
        context extends KZ_VRED_TS_DEADLINES {
            decision_time @2026-09-01T12:00:00+05:00;
            knowledge_time @2026-09-01T12:00:00+05:00;
            legal_time @2026-09-01;
            timezone "Asia/Qyzylorda";
            calendar "urn:kz:corpus:clir:official-calendar#snapshot-2026";
        }
        assert poterpevshiy_po_raschetu(entity_ref("urn:parity:vred:raschet-1"), entity_ref("urn:parity:vred:poterpevshiy-1")) { id "S16-a0"; origin case_input; }
        assert otchet_poluchen_poterpevshim(entity_ref("urn:parity:vred:raschet-1"), @2024-12-31) { id "S16-a1"; origin case_input; }
    }
    evaluate collect day: Date where predel_otmetki_poterpevshego(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(0);
    evaluate collect day: Date where predel_otveta_strakhovshchika(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(0);
    evaluate collect day: Date where predel_predostavleniya_otcheta(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(0);
    evaluate truth(otchet_ne_predostavlen_v_srok(entity_ref("urn:parity:vred:raschet-1")));
    expect truth_status == NEITHER;
    evaluate truth(pravo_na_vyplatu_po_punktu_3_1_stati_22_zakona(entity_ref("urn:parity:vred:poterpevshiy-1"), entity_ref("urn:parity:vred:raschet-1")));
    expect truth_status == NEITHER;
}

// S17 — cl. 3 para. 6: Both grounds for non-provision at once — one conclusion, one right.
test "urn:query:vred-parity:S17" {
    given {
        context extends KZ_VRED_TS_DEADLINES {
            decision_time @2026-09-01T12:00:00+05:00;
            knowledge_time @2026-09-01T12:00:00+05:00;
            legal_time @2026-09-01;
            timezone "Asia/Qyzylorda";
            calendar "urn:kz:corpus:clir:official-calendar#snapshot-2026";
        }
        assert poterpevshiy_po_raschetu(entity_ref("urn:parity:vred:raschet-1"), entity_ref("urn:parity:vred:poterpevshiy-1")) { id "S17-a0"; origin case_input; }
        assert otchet_poluchen_poterpevshim(entity_ref("urn:parity:vred:raschet-1"), @2026-09-09) { id "S17-a1"; origin case_input; }
        assert osmotr_osushchestvlen_strakhovshchikom(entity_ref("urn:parity:vred:raschet-1"), @2026-09-01) { id "S17-a2"; origin case_input; }
        assert not otchet_predostavlen_poterpevshemu(entity_ref("urn:parity:vred:raschet-1")) { id "S17-a3"; origin case_input; }
        assert den_rassmotreniya_dela(entity_ref("urn:parity:vred:raschet-1"), @2026-09-09) { id "S17-a4"; origin case_input; }
    }
    evaluate collect day: Date where predel_otmetki_poterpevshego(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(1);
    evaluate truth(predel_otmetki_poterpevshego(entity_ref("urn:parity:vred:raschet-1"), @2026-09-14));
    expect truth_status == TRUE_ONLY;
    evaluate collect day: Date where predel_otveta_strakhovshchika(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(0);
    evaluate collect day: Date where predel_predostavleniya_otcheta(entity_ref("urn:parity:vred:raschet-1"), day);
    expect result_kind == COLLECTION;
    expect collected_count(1);
    evaluate truth(predel_predostavleniya_otcheta(entity_ref("urn:parity:vred:raschet-1"), @2026-09-08));
    expect truth_status == TRUE_ONLY;
    evaluate truth(otchet_ne_predostavlen_v_srok(entity_ref("urn:parity:vred:raschet-1")));
    expect truth_status == TRUE_ONLY;
    evaluate truth(pravo_na_vyplatu_po_punktu_3_1_stati_22_zakona(entity_ref("urn:parity:vred:poterpevshiy-1"), entity_ref("urn:parity:vred:raschet-1")));
    expect truth_status == TRUE_ONLY;
}

