language "law.core" version "0.1";
package kz.corpus.budget_code version "0.1.0";
namespace "urn:kz:corpus:clir:budget-code";

// Порождённые сценарии-свидетельства каталога вопросов (DECISION-0190):
// мир выполняет посылки правила, запрос спрашивает его вывод.
// Собраны из CLIR пакета; правка руками — только вместе с каталогом.

// Статья 133 пункт 2. Государственные учреждения и уполномоченные органы обязаны составлять годовую консолидированную финансовую отчётность в порядке, установленн
test "CATALOG-CONSOLIDATEDREPORTDUTY" {
    given {
        context { decision_time @2026-08-25T12:00:00Z; knowledge_time @2026-08-25T12:00:00Z; legal_time @2026-08-25; timezone "Asia/Almaty"; }
        assert "cat-0": consolidated_report_obliged(entity_ref("urn:kz:catalog:consolidatedreportduty:v0"), entity_ref("urn:kz:catalog:consolidatedreportduty:v1")) { origin case_input; }
    }
    evaluate positions();
    expect evaluation_status == COMPUTED;
    expect position(PrepareConsolidatedReport, ACTIVE);
}
